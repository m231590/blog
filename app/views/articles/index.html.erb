<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting!

<div class="alert alert-success">Check back often for updates!!</div>
<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <%= link_to article.title, article %>
        </li>
    <% end %>
</ul>

<div class="container">
    <div class="row"> <!--Articles -->
        <div class="col">
            <h1 class="display-1">Welcome to Charles' Blog</h1>
        </div>
    </div>
    <div class="row">
        <% @articles.each do |article| %>
        <div class="col">
            <h3><%= article.title%></h3>
            <p><%= article.body%></p>
        </div>
    <%end%>
    </div>
</div>

<a href="www.amazon.com" class="btn btn-primary">Go to Amazon</a>
<button type="button" class="btn btn-primary">Primary</button>
<%= link_to "New Article", new_article_path %>